{% for block in blocks %}
    {% if block.isAsync %}
        {# use hinclude in future #}
        {{render(controller('ApplicationCMSBundle:Page:block', {'block': block}))}}
    {% else %}
        {{render(controller('ApplicationCMSBundle:Page:block', {'block': block}))}}
    {% endif %}
{% endfor %}